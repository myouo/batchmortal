# BatchMortal

## Usage 使用方法

安装依赖

```bash
pip install -r requirements.txt
```

batch analyzing / 启动批量分析（速度稍慢）

```bash
python .\main.py {username} --limit {limit_num，分析牌谱的限制数量} --modes {12为玉南，9为金南，16为王座南} --headless
```

help / 帮助

```
python main.py --help
```

basing MIT.
